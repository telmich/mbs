 * User auth via ldap/krb?
 * reservation checks
 * Remove hostnames / display after booking

Reservations
------------

"select_by_type_and_date" variant:
   bookable = true
   for each selected type do |type|
      mt = find_free_nodes_of type count begin end
      if mt
         nodes << mt
      else
         date = find_first_free_date_for_nodes_of type count begin end
         bookable = false
      end
   end

 * Select machines:
  * if(machine_usable && Reservation.last.booking.end <= Neu

 * Create a dynamic array similar to the one in sdb?
   -> Add machines
      -> [no] [type] 

Users
-----
LDAP?
Mail verification?

Mail:
   Create unverified user
