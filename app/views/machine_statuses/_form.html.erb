<%= form_for(@machine_status) do |f| %>
  <% if @machine_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_status.errors.count, "error") %> prohibited this machine_status from being saved:</h2>

      <ul>
      <% @machine_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
