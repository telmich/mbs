<h1>Listing bookings</h1>

<table>
  <tr>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bookings.each do |booking| %>
  <tr>
    <td><%= booking.user_id %></td>
    <td><%= link_to 'Show', booking %></td>
    <td><%= link_to 'Edit', edit_booking_path(booking) %></td>
    <td><%= link_to 'Destroy', booking, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Booking', new_booking_path %>

<h1>Listing bookings with Reservations</h1>
<table>
  <tr>
    <th>User</th>
    <th>Booking #</th>
    <th></th>
    <th></th>
  </tr>

<% @booking_reservations.each do |rv| %>
  <tr>
    <td><%= rv.user_id %></td>
    <td><%= booking.id %></td>
    <td><%= link_to 'Edit', edit_booking_path(booking) %></td>
    <td><%= link_to 'Destroy', booking, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>


<h1>Welcome to the reservation system</h1>

<ul>
   <li><%= link_to 'Users', users_path %>
   <li><%= link_to 'Machines', machines_path %>
   <li><%= link_to 'Reservations', reservations_path %>
</ul>

<h2>Reserve machines </h2)
<% form_for :booking, do %>

   <br><%= label_tag(:user_id, "User:") %><%= text_field(:user, :name) %>
   <br><%= label_tag(:date_start, "Begin:") %><%= select_date Date.today, :prefix => :date_start %> 
   <br><%= label_tag(:date_start, "End:") %><%= select_date Date.today, :prefix => :date_end %> 

   <table>
   <% @machines.each do |machine| %>
      <tr class="entry">
         <td><%= machine.title %></td>
         <td><%= machine.description %></td>
         <td><%= check_box_tag "bookings[machine_ids][]", machine.id -%> </td>
      </tr>
   <% end %>
   </table>
   <%= submit_tag("Book!") %> 
<% end %> 

