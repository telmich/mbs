<%= form_for(@gesamt) do |f| %>
  <% if @gesamt.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@gesamt.errors.count, "error") %> prohibited this gesamt from being saved:</h2>
    <ul>
    <% @gesamt.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
